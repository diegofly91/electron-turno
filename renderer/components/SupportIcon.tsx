import SupportAgentIcon from '@mui/icons-material/SupportAgent';
import { blue } from '@mui/material/colors';

const SupportIcon = () => {

    return <SupportAgentIcon 
                sx={{ fontSize: "100px", color: blue[500] }}
           />
}

export default SupportIcon;